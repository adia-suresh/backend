{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'java -Dserver.port=$PORT -Dserver.address=0.0.0.0 -jar target/*.jar'",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
